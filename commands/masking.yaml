---
description: 'Trigger scmask jenkins job'
help: |
  Trigger scmasking Jenkins job.
  parameters:
      MY_NAME: my name
      PR: pr number
commands:
  _property: # as default subcommand properties
    type: Jenkins
    server: https://devopscn.rtp.raleigh.ibm.com:8443
    owner: zzlzhang@cn.ibm.com
    token: '141c7c40e9d599900e0b72e6e8e1d336'
    allow_channels: [] # specify the channels where the command can be run
    permission: [zzlzhang, gbyukg]
  default:
    property:
      action: build # Plugin's function name, default is subcommand's name
      job_name: slack_bot
      before_message:
      after_message:
    params:
      # _lock: True # if true: default parameters can not be overwrite
      MY_NAME: zzlzhag
      PR:
        - R41
        - R42
      DB: true
